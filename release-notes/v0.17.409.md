# Proma v0.17.409 更新

## 变更

- 提示词末尾追加的时间戳精度由「分钟」改为「日期」（形如 `当前日期: 2026年08月17日星期一`），开关名同步改为「追加日期和用户名」。模型仍然知道今天是几号、星期几，只是不再知道具体时刻。
- 因此上一版提示的「建议关掉该开关以免提示词缓存失效」**不再需要**，可以放心重新打开：原先精确到分钟的时间戳会让系统提示词每分钟变一次，进而让整段缓存前缀作废；改成日期后前缀能稳定复用。实测开着该开关连续跨分钟提问，缓存均正常命中。
