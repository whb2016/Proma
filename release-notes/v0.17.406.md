# Proma v0.17.406 更新

## Bug 修复

- 修复翻译页原文里的空行在译文中被压成普通换行：段落之间只多出 6px 间距，在 24px 的行距上肉眼看不出差别，于是「原文空了一行」显示成「只换了一行」。译文的段间距改为整整一行，行高也对齐左侧原文框，空行和单换行现在能明显区分、左右两栏的行也能对上。
  - 上一版只修了「单换行被吞掉」，这一版补上「空行被压扁」，两者是同一个渲染配置的两半。
  - 如果你之前为了绕过这两个问题改过翻译提示词（比如要求「每行之间留一个空行」），请在设置里点「重置」换回默认提示词，否则译文会变成三倍行距。
